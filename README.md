# 简介
每次换服务器总是需要手动配置环境，使用该脚本，实现自动安装python/java/docker
# 测试系统
**centos8**
# 使用方法：
1. `chmod +x centos.sh`
2. `./centos.sh`
3. 重启系统
# 验证是否成功
1. `docker --version`
2. `java -version`
3. `python3.13 --version`
